# Backend — FastAPI 🚀

REST API сервис для управления пользователями и мероприятиями.

## 📁 Структура

```
backend/
├── main.py                 # Точка входа FastAPI приложения
├── pyproject.toml          # Зависимости проекта
├── alembic.ini             # Конфигурация Alembic
├── alembic/                # Миграции БД
│   ├── env.py              # Настройка Alembic для async
│   ├── script.py.mako      # Шаблон миграции
│   └── versions/           # Файлы миграций
├── src/
│   ├── __init__.py
│   ├── config.py           # Настройки (pydantic-settings)
│   ├── database.py         # SQLAlchemy engine, session
│   ├── logger.py           # Настройка loguru
│   ├── admin.py            # Админ-панель (SQLAdmin)
│   ├── users/              # Модуль пользователей
│   │   ├── __init__.py
│   │   ├── models.py       # SQLAlchemy модель User
│   │   ├── schemas.py      # Pydantic схемы
│   │   ├── crud.py         # CRUD операции
│   │   └── router.py       # API эндпоинты
│   └── events/             # Модуль мероприятий
│       ├── __init__.py
│       ├── models.py       # Event + EventParticipant
│       ├── schemas.py      # Pydantic схемы
│       ├── crud.py         # CRUD операции
│       └── router.py       # API эндпоинты
└── Dockerfile              # Docker образ
```

## 🗃 Модели данных

### User (Пользователь)
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | int | Первичный ключ |
| `telegram_id` | bigint | ID в Telegram (уникальный, индекс) |
| `username` | str | Username в Telegram |
| `first_name` | str | Имя |
| `created_at` | datetime | Дата регистрации |

### Event (Мероприятие)
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | int | Первичный ключ |
| `title` | str | Название |
| `description` | text | Описание |
| `date` | datetime | Дата и время |
| `location` | str | Место проведения |
| `creator_id` | int | FK на User (индекс) |
| `created_at` | datetime | Дата создания |

### EventParticipant (Участники)
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | int | Первичный ключ |
| `user_id` | int | FK на User (индекс) |
| `event_id` | int | FK на Event (индекс) |
| `joined_at` | datetime | Дата присоединения |

## 🚀 Запуск

```bash
# Установка зависимостей
uv sync

# Запуск в режиме разработки
uv run uvicorn main:app --reload

# Запуск продакшен
uv run uvicorn main:app --host 0.0.0.0 --port 8000
```

## 🔧 Переменные окружения

Создайте файл `.env`:

**Для локальной разработки (SQLite):**
```env
DATABASE_URL=sqlite+aiosqlite:///./app.db
DEBUG=true
API_HOST=0.0.0.0
API_PORT=8000
```

**Для Docker (PostgreSQL):**
```env
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/vmeste_db
DEBUG=false
API_HOST=0.0.0.0
API_PORT=8000
```

> **Примечание**: В Docker Compose `DATABASE_URL` формируется автоматически из переменных PostgreSQL.

## 📝 Миграции

```bash
# Создать миграцию
uv run alembic revision --autogenerate -m "описание изменений"

# Применить все миграции
uv run alembic upgrade head

# Откатить последнюю миграцию
uv run alembic downgrade -1

# Посмотреть текущую версию
uv run alembic current
```

## 📚 API документация

После запуска доступна по адресам:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **OpenAPI JSON**: http://localhost:8000/openapi.json

## 🔐 Админ-панель

Проект включает админ-панель на базе [SQLAdmin](https://github.com/aminalaee/sqladmin) для управления данными через веб-интерфейс.

### Доступ

Админ-панель доступна по адресу: **http://localhost:8000/admin**

### Авторизация

По умолчанию:
- **Логин**: `admin`
- **Пароль**: `admin`

**⚠️ Важно**: Обязательно измените пароль в продакшене через переменные окружения!

### Настройка через .env

```env
ADMIN_USERNAME=my_admin
ADMIN_PASSWORD=secure_password_123
ADMIN_SECRET_KEY=your-secret-key-for-sessions-min-32-chars
```

### Возможности

- ✅ Просмотр всех пользователей, мероприятий и участников
- ✅ Создание, редактирование и удаление записей
- ✅ Поиск по полям
- ✅ Сортировка данных
- ✅ Детальный просмотр записей
- ✅ Защита паролем

### Модели в админке

1. **Пользователи** (`/admin/user`)
   - Просмотр всех пользователей
   - Редактирование данных
   - Поиск по Telegram ID, username, имени

2. **Мероприятия** (`/admin/event`)
   - Управление тренировками
   - Редактирование названия, описания, даты, места
   - Поиск по названию, описанию, месту

3. **Участники** (`/admin/eventparticipant`)
   - Просмотр связей пользователей и мероприятий
   - Управление участием

### Безопасность

В продакшене:
1. Используйте сильные пароли
2. Настройте HTTPS
3. Ограничьте доступ по IP (через reverse proxy)
4. Регулярно меняйте пароль
