version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: vmeste_postgres
    env_file:
      - .env  # Симлинк на корневой .env файл
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-vmeste_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    container_name: vmeste_backend
    env_file:
      - .env  # Симлинк на корневой .env файл
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-vmeste_db}
      - DEBUG=${DEBUG:-false}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  bot:
    build:
      context: ..
      dockerfile: bot/Dockerfile
    # Не указываем container_name для поддержки scaling через docker-compose scale
    env_file:
      - .env  # Симлинк на корневой .env файл
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - API_BASE_URL=http://backend:8000/api/v1
      - DEBUG=${DEBUG:-false}
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    # Для масштабирования используйте:
    # - Переменную окружения: BOT_REPLICAS=3 ./start.sh
    # - Или напрямую: docker-compose up --scale bot=N

volumes:
  postgres_data:
    driver: local
